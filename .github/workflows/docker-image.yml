name: bpmn-translator-and-verifier Docker image generation

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag bpmn-translator-and-verifier:$(date +%s)
